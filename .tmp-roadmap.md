# Temporary Roadmap

Updated: 2026-04-27

This file is a temporary checkpoint so the current execution order survives an interruption.

## Ordered Steps

- [x] Step 1: Expand the official Playwright smoke suite to cover search and filter interactions plus one newly added algorithm route.
- [x] Step 2: Improve MDX content quality with reusable callout and side-by-side example components for JavaScript and TypeScript snippets.
- [x] Step 3: Expand the catalog with additional high-frequency patterns.
- [x] Step 4: Add Vercel deployment validation by linking the project and running a preview deploy check.

## Operating Notes

- Run `npm run cleanup:dev` before and after local validation.
- Keep only one repo-owned dev server alive at a time.
- Use Playwright-based checks instead of VS Code browser tools.