---
title: "Primorial of a number"
slug: "primorial-of-a-number"
category: "Number Theory"
description: "Compute the primorial Pn# as the product of the first n prime numbers."
techniques: ["Prime Numbers", "Number Theory", "Primality Testing"]
complexity:
  time: "O(n * sqrt(p_n))"
  space: "O(1)"
  notes: "Trial division is sufficient for interview-sized inputs; results grow quickly so BigInt is used"
---

## Overview

Primorial of `n` (written `Pn#`) is the product of the first `n` prime numbers.

Examples:

- `P1# = 2`
- `P3# = 2 * 3 * 5 = 30`
- `P5# = 2 * 3 * 5 * 7 * 11 = 2310`

## Approach

Scan integers from 2 upward, keep only primes, and multiply until we collect `n` primes.

```js
function isPrime(x) {
  if (x < 2) return false;
  if (x === 2) return true;
  if (x % 2 === 0) return false;

  for (let d = 3; d * d <= x; d += 2) {
    if (x % d === 0) return false;
  }
  return true;
}

function primorial(n) {
  if (!Number.isInteger(n) || n < 0) return -1n;
  if (n === 0) return 1n;

  let found = 0;
  let candidate = 2;
  let result = 1n;

  while (found < n) {
    if (isPrime(candidate)) {
      result *= BigInt(candidate);
      found++;
    }
    candidate++;
  }

  return result;
}

console.assert(primorial(1) === 2n, "P1# = 2");
console.assert(primorial(3) === 30n, "P3# = 30");
console.assert(primorial(5) === 2310n, "P5# = 2310");
console.assert(primorial(0) === 1n, "empty product convention");
```

## Notes

- Primorial is analogous to factorial, but multiplies primes only.
- Values grow fast, so `BigInt` avoids overflow.
